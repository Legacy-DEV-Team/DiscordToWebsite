module.exports = {
    botToken: 'YOUR_BOT_TOKEN',      // Replace with your bot's token
    port: 5000,                      // Set the port your server will listen on
    ip: '0.0.0.0',                   // Use '0.0.0.0' for all IPs or specify a specific IP if needed
  };
  